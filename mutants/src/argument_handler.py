#!/usr/bin/env python3

###########################    Description    ##################################
# Parsers for handling SeqSender input
################################################################################

import argparse
from src.settings import ORGANISM_CHOICES
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
	"""Forward call to original or mutated function, depending on the environment"""
	import os # type: ignore
	mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
	if mutant_under_test == 'fail': # type: ignore
		from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
		raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
	elif mutant_under_test == 'stats': # type: ignore
		from mutmut.__main__ import record_trampoline_hit # type: ignore
		record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
		# (for class methods, orig is bound and thus does not need the explicit self argument)
		result = orig(*call_args, **call_kwargs) # type: ignore
		return result # type: ignore
	prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
	if not mutant_under_test.startswith(prefix): # type: ignore
		result = orig(*call_args, **call_kwargs) # type: ignore
		return result # type: ignore
	mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
	if self_arg is not None: # type: ignore
		# call to a class method where self is not bound
		result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
	else:
		result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
	return result # type: ignore

def args_parser():
	args = []# type: ignore
	kwargs = {}# type: ignore
	return _mutmut_trampoline(x_args_parser__mutmut_orig, x_args_parser__mutmut_mutants, args, kwargs, None)

def x_args_parser__mutmut_orig():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_1():
	"""
	Argument parser setup and build.
	"""
	parser = None
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_2():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=None,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_3():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description=None)
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_4():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_5():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									)
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_6():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="XXGenomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.XX")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_7():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="genomic tool to simplify/automate the process of submitting organism samples to public repositories. with built-in tools to create/submit/link/log organism samples for the databases: biosample, sra, genbank, and gisaid.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_8():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="GENOMIC TOOL TO SIMPLIFY/AUTOMATE THE PROCESS OF SUBMITTING ORGANISM SAMPLES TO PUBLIC REPOSITORIES. WITH BUILT-IN TOOLS TO CREATE/SUBMIT/LINK/LOG ORGANISM SAMPLES FOR THE DATABASES: BIOSAMPLE, SRA, GENBANK, AND GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_9():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = None
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_10():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=None)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_11():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=True)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_12():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = None
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_13():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=None)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_14():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=True)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_15():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = None
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_16():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=None)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_17():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=True)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_18():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = None
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_19():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=None)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_20():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=True)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_21():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = None
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_22():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=None)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_23():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=True)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_24():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = None
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_25():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=None)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_26():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=True)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_27():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = None
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_28():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=None)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_29():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=True)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_30():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = None
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_31():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=None)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_32():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=True)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_33():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = None
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_34():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=None)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_35():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=True)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_36():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = None
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_37():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=None)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_38():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=True)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_39():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = None
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_40():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=None)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_41():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=True)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_42():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = None
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_43():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=None)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_44():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=True)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_45():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = None
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_46():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=None)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_47():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=True)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_48():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = None
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_49():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=None)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_50():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=True)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_51():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument(None, "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_52():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", None,
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_53():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help=None,
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_54():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action=None,
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_55():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const=None,
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_56():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default=None)
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_57():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_58():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_59():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_60():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_61():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_62():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		)
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_63():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("XX--biosampleXX", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_64():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--BIOSAMPLE", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_65():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "XX-bXX",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_66():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-B",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_67():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="XXCreate/Submit BioSample data.XX",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_68():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="create/submit biosample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_69():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="CREATE/SUBMIT BIOSAMPLE DATA.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_70():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="XXstore_constXX",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_71():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="STORE_CONST",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_72():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="XXBIOSAMPLEXX",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_73():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="biosample",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_74():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="XXXX")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_75():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument(None, "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_76():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", None,
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_77():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help=None,
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_78():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action=None,
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_79():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const=None,
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_80():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default=None)
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_81():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_82():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_83():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_84():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_85():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_86():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		)
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_87():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("XX--sraXX", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_88():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--SRA", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_89():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "XX-sXX",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_90():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-S",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_91():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="XXCreate/Submit SRA data.XX",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_92():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="create/submit sra data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_93():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="CREATE/SUBMIT SRA DATA.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_94():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="XXstore_constXX",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_95():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="STORE_CONST",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_96():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="XXSRAXX",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_97():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="sra",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_98():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="XXXX")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_99():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument(None, "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_100():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", None,
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_101():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help=None,
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_102():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action=None,
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_103():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const=None,
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_104():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default=None)
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_105():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_106():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_107():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_108():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_109():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_110():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		)
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_111():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("XX--genbankXX", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_112():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--GENBANK", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_113():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "XX-nXX",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_114():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-N",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_115():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="XXCreate/Submit GenBank data. (requires --fasta_file)XX",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_116():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="create/submit genbank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_117():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="CREATE/SUBMIT GENBANK DATA. (REQUIRES --FASTA_FILE)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_118():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="XXstore_constXX",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_119():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="STORE_CONST",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_120():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="XXGENBANKXX",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_121():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="genbank",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_122():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="XXXX")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_123():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument(None, "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_124():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", None,
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_125():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help=None,
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_126():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action=None,
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_127():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const=None,
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_128():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default=None)
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_129():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_130():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_131():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_132():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_133():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_134():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		)
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_135():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("XX--gisaidXX", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_136():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--GISAID", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_137():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "XX-gXX",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_138():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-G",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_139():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="XXCreate/Submit GISAID data. (requires --fasta_file)XX",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_140():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="create/submit gisaid data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_141():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="CREATE/SUBMIT GISAID DATA. (REQUIRES --FASTA_FILE)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_142():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="XXstore_constXX",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_143():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="STORE_CONST",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_144():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="XXGISAIDXX",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_145():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="gisaid",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_146():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="XXXX")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_147():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument(None,
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_148():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help=None,
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_149():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=None,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_150():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default=None,
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_151():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=None)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_152():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument(help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_153():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_154():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_155():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_156():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_157():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("XX--organismXX",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_158():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--ORGANISM",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_159():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="XXType of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.XX",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_160():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="type of organism data. listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'other' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_161():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="TYPE OF ORGANISM DATA. LISTED ORGANISM OPTIONS HAVE UNIQUE SUBMISSIONS OPTIONS/PROCESSES, IF YOUR SPECIFIC ORGANISM IS NOT LISTED, USE 'OTHER' FOR OPTIONS AVAILABLE TO ALL ORGANISMS.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_162():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="XXXX",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_163():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=False)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_164():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument(None,
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_165():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help=None,
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_166():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action=None,
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_167():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=None,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_168():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=None)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_169():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument(help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_170():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_171():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_172():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_173():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_174():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("XX--skip_validationXX",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_175():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--SKIP_VALIDATION",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_176():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="XXSkip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.XX",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_177():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="skip initial validation for metadata file. validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. warning, this can cause unexpected errors using seqsender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_178():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="SKIP INITIAL VALIDATION FOR METADATA FILE. VALIDATION WILL STILL OCCUR FOR THE 'CONFIG_FILE' AND FOR ANY SUBSEQUENT SUBMISSIONS MADE VIA 'SUBMISSION_STATUS'. WARNING, THIS CAN CAUSE UNEXPECTED ERRORS USING SEQSENDER IF REQUIRED COLUMNS ARE MISSING.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_179():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="XXstore_constXX",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_180():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="STORE_CONST",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_181():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=True,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_182():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=False)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_183():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument(None,
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_184():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help=None,
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_185():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=None)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_186():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument(help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_187():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_188():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_189():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("XX--submission_nameXX",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_190():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--SUBMISSION_NAME",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_191():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="XXUnique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.XX",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_192():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="unique name for the submission of your data. reusing the same name can cause issues during the submission process. a folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_193():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="UNIQUE NAME FOR THE SUBMISSION OF YOUR DATA. REUSING THE SAME NAME CAN CAUSE ISSUES DURING THE SUBMISSION PROCESS. A FOLDER WILL BE CREATED AT: 'SUBMISSION_DIR/SUBMISSION_NAME'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_194():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=False)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_195():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument(None,
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_196():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help=None)
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_197():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument(help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_198():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		)
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_199():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("XX--submission_nameXX",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_200():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--SUBMISSION_NAME",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_201():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="XXUnique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.XX")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_202():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="unique name for the submission of your data. this is an optional field if you want seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_203():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="UNIQUE NAME FOR THE SUBMISSION OF YOUR DATA. THIS IS AN OPTIONAL FIELD IF YOU WANT SEQSENDER TO ONLY UPDATE THE SPECIFIED SUBMISSION IN THE 'SUBMISSION_LOG.CSV'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_204():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument(None,
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_205():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help=None,
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_206():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=None)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_207():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument(help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_208():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_209():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_210():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("XX--submission_dirXX",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_211():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--SUBMISSION_DIR",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_212():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="XXOutput directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.XX",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_213():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="output directory where all files for your submission will be stored. a folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, seqsender will stage each step of the submission process automatically, and where seqsender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_214():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="OUTPUT DIRECTORY WHERE ALL FILES FOR YOUR SUBMISSION WILL BE STORED. A FOLDER WILL BE CREATED AT '<SUBMISSION_DIR>/<SUBMISSION_NAME>'; THIS IS THE LOCATION WHERE: ALL OF THE SUBMISSION FILES WILL BE CREATED, SEQSENDER WILL STAGE EACH STEP OF THE SUBMISSION PROCESS AUTOMATICALLY, AND WHERE SEQSENDER WILL GENERATE ALL THE OUTPUT FROM YOUR SUBMISSION.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_215():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=False)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_216():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument(None,
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_217():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help=None,
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_218():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=None)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_219():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument(help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_220():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_221():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_222():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("XX--config_fileXX",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_223():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--CONFIG_FILE",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_224():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="XXConfig file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.XX",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_225():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="config file to be used in the creation/submission of your samples. seqsender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure seqsender maintains access to this file. input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_226():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="CONFIG FILE TO BE USED IN THE CREATION/SUBMISSION OF YOUR SAMPLES. SEQSENDER WILL STORE THIS FILE LOCATION IN YOUR 'SUBMISSION_LOG.CSV' WHERE IT WILL USE IT TO MANAGE YOUR SUBMISSION, BE CAREFUL WHEN MODIFYING AND ENSURE SEQSENDER MAINTAINS ACCESS TO THIS FILE. INPUT EITHER FULL FILE PATH OR IF JUST FILE NAME IT MUST BE STORED AT '<SUBMISSION_DIR>/<SUBMISSION_NAME>/<CONFIG_FILE>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_227():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=False)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_228():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument(None,
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_229():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help=None,
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_230():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=None)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_231():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument(help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_232():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_233():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_234():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("XX--keyXX",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_235():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--KEY",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_236():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="XXKey provided to access credentials stored in config file.XX",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_237():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_238():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="KEY PROVIDED TO ACCESS CREDENTIALS STORED IN CONFIG FILE.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_239():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=False)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_240():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument(None,
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_241():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help=None,
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_242():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs=None,
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_243():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=None)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_244():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument(help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_245():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_246():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_247():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_248():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("XX--keyXX",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_249():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--KEY",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_250():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="XXKey provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.XX",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_251():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="key provided to access credentials stored in config file. multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_252():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="KEY PROVIDED TO ACCESS CREDENTIALS STORED IN CONFIG FILE. MULTIPLE KEYS MAY BE PROVIDED IF MULTIPLE CONFIG FILES ARE BEING CHECKED.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_253():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='XX+XX',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_254():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=False)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_255():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument(None,
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_256():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help=None,
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_257():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=None)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_258():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument(help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_259():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_260():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_261():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("XX--config_fileXX",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_262():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--CONFIG_FILE",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_263():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="XXConfig file to be used in the creation/submission of your samples. Input full file path.XX",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_264():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="config file to be used in the creation/submission of your samples. input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_265():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="CONFIG FILE TO BE USED IN THE CREATION/SUBMISSION OF YOUR SAMPLES. INPUT FULL FILE PATH.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_266():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=False)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_267():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument(None,
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_268():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help=None,
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_269():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=None)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_270():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument(help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_271():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_272():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_273():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_274():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("XX--keyXX",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_275():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--KEY",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_276():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="XXEncryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.XX",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_277():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="encryption key to use when encrypting your credentials. allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_278():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="ENCRYPTION KEY TO USE WHEN ENCRYPTING YOUR CREDENTIALS. ALLOWS THE USER TO REUSE THE ENCRYPTION KEY FOR MULTIPLE CONFIG FILES FOR USERS WHERE MULTIPLE CONFIG FILES ARE NEEDED FOR DIFFERENT SUBMISSION CRITERIA.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_279():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=True)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_280():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument(None,
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_281():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help=None,
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_282():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=None)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_283():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument(help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_284():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_285():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_286():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("XX--metadata_fileXX",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_287():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--METADATA_FILE",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_288():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="XXMetadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.XX",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_289():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="metadata file to be used in the creation/submission of your samples. input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_290():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="METADATA FILE TO BE USED IN THE CREATION/SUBMISSION OF YOUR SAMPLES. INPUT EITHER FULL FILE PATH OR IF JUST FILE NAME IT MUST BE STORED AT '<SUBMISSION_DIR>/<SUBMISSION_NAME>/<METADATA_FILE>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_291():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=False)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_292():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument(None,
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_293():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help=None,
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_294():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument(help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_295():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_296():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_297():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("XX--fasta_fileXX",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_298():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--FASTA_FILE",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_299():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="XXFasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.XX",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_300():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_301():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="FASTA FILE USED TO GENERATE SUBMISSION FILES; FASTA HEADER SHOULD MATCH THE COLUMN 'SEQUENCE_NAME' STORED IN YOUR METADATA. INPUT EITHER FULL FILE PATH OR IF JUST FILE NAME IT MUST BE STORED AT '<SUBMISSION_DIR>/<SUBMISSION_NAME>/<FASTA_FILE>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_302():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument(None,
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_303():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help=None,
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_304():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action=None,
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_305():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=None,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_306():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=None)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_307():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument(help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_308():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_309():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_310():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_311():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_312():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("XX--table2asnXX",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_313():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--TABLE2ASN",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_314():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="XXPerform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.XX",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_315():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="perform a table2asn submission instead of genbank ftp submission for organism choices 'flu' or 'cov'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_316():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="PERFORM A TABLE2ASN SUBMISSION INSTEAD OF GENBANK FTP SUBMISSION FOR ORGANISM CHOICES 'FLU' OR 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_317():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="XXstore_constXX",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_318():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="STORE_CONST",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_319():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=True,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_320():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=False)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_321():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument(None,
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_322():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help=None,
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_323():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument(help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_324():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_325():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_326():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("XX--gff_fileXX",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_327():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--GFF_FILE",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_328():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="XXAnnotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').XX",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_329():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'flu', or 'cov').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_330():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="ANNOTATION FILE ONLY AVAILABLE FOR TABLE2ASN SUBMISSIONS. (REQUIRES '--TABLE2ASN' FOR ORGANISM CHOICES 'FLU', OR 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_331():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument(None,
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_332():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help=None,
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_333():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action=None,
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_334():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=None,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_335():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=None)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_336():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument(help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_337():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_338():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_339():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_340():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_341():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("XX--testXX",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_342():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--TEST",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_343():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="XXPerform a test submission.XX",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_344():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_345():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="PERFORM A TEST SUBMISSION.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_346():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="XXstore_constXX",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_347():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="STORE_CONST",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_348():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=True,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_349():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=False)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_350():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument(None,
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_351():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help=None,
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_352():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument(help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_353():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_354():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_355():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("XX--publication_titleXX",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_356():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--PUBLICATION_TITLE",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_357():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="XXPublication Title associated with sample submission. For GenBank only, overwrites value given via config file.XX",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_358():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="publication title associated with sample submission. for genbank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_359():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="PUBLICATION TITLE ASSOCIATED WITH SAMPLE SUBMISSION. FOR GENBANK ONLY, OVERWRITES VALUE GIVEN VIA CONFIG FILE.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_360():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument(None,
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_361():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help=None,
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_362():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=None)

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_363():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument(help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_364():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_365():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_366():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		)

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_367():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("XX--publication_statusXX",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_368():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--PUBLICATION_STATUS",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_369():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="XXStatus of publication associated with sample submission. For GenBank only, overwrites value given via config file.XX",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_370():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="status of publication associated with sample submission. for genbank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_371():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="STATUS OF PUBLICATION ASSOCIATED WITH SAMPLE SUBMISSION. FOR GENBANK ONLY, OVERWRITES VALUE GIVEN VIA CONFIG FILE.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_372():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["XXUnpublishedXX", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_373():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_374():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["UNPUBLISHED", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_375():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "XXIn-pressXX", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_376():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "in-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_377():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "IN-PRESS", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_378():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "XXPublishedXX"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_379():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_380():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "PUBLISHED"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_381():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = None

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_382():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest=None)

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_383():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="XXcommandXX")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_384():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="COMMAND")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_385():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = None

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_386():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		None,
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_387():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=None,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_388():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description=None,
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_389():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=None
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_390():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_391():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_392():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_393():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_394():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"XXprepXX",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_395():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"PREP",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_396():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="XXGenerate all files required to submit to databases selected.XX",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_397():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_398():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="GENERATE ALL FILES REQUIRED TO SUBMIT TO DATABASES SELECTED.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_399():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = None

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_400():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		None,
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_401():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=None,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_402():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description=None,
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_403():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=None
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_404():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_405():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_406():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_407():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_408():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"XXsubmitXX",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_409():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"SUBMIT",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_410():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="XXGenerate all files required and begin the submission process to databases selected.XX",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_411():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_412():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="GENERATE ALL FILES REQUIRED AND BEGIN THE SUBMISSION PROCESS TO DATABASES SELECTED.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_413():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = None

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_414():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		None,
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_415():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=None,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_416():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description=None,
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_417():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=None
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_418():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_419():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_420():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_421():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_422():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"XXsubmission_statusXX",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_423():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"SUBMISSION_STATUS",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_424():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="XXChecks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).XX",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_425():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_426():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="CHECKS THE SUBMISSION STATUS FOR (ALL/SPECIFIED <SUBMISSION_NAME>) SUBMISSION('S) WHICH WILL FOR EACH DATABASE: UPDATE THE STATUS OF THE SUBMISSION('S), DOWNLOAD OUTPUT FILE('S), SUBMIT TO SUBSEQUENT SPECIFIED DATABASES IF LINKING INFORMATION REQUIRES OUTPUT OF PREVIOUS DATABASE('S).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_427():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = None

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_428():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		None,
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_429():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=None,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_430():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description=None,
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_431():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=None
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_432():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_433():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_434():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_435():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_436():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"XXload_credentialsXX",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_437():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"LOAD_CREDENTIALS",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_438():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="XXLoads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.XX",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_439():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="loads and encrypts your submission credentials into your submission config file. provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_440():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="LOADS AND ENCRYPTS YOUR SUBMISSION CREDENTIALS INTO YOUR SUBMISSION CONFIG FILE. PROVIDES A SECRET KEY TO SECURELY ACCESS YOUR CREDENTIALS FOR SUBMISSION.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_441():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = None

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_442():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		None,
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_443():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=None,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_444():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description=None,
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_445():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=None
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_446():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_447():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_448():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_449():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_450():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"XXtest_dataXX",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_451():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"TEST_DATA",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_452():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="XXReturns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.XX",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_453():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_454():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="RETURNS A SET OF TEST DATA EXAMPLES (CONFIG_FILE, METADATA_FILE, FASTA_FILE, ETC.) TO THE SPECIFIED 'SUBMISSION_DIR' BASED ON ORGANISM AND DATABASE SELECTIONS.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_455():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = None

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_456():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		None,
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_457():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=None,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_458():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description=None
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_459():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_460():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_461():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_462():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"XXupdate_biosampleXX",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_463():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"UPDATE_BIOSAMPLE",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_464():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="XXDownloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database.XX"
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_465():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="downloads the biosample package xml from ncbi and updates seqsender's metadata schema options for the biosample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_466():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="DOWNLOADS THE BIOSAMPLE PACKAGE XML FROM NCBI AND UPDATES SEQSENDER'S METADATA SCHEMA OPTIONS FOR THE BIOSAMPLE DATABASE."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_467():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = None

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_468():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		None,
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_469():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=None,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_470():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description=None
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_471():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_472():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_473():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_474():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"XXtest_network_connectionXX",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_475():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"TEST_NETWORK_CONNECTION",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_476():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="XXRuns a series of test network connections to NCBI and GISAID to troubleshoot submission issues.XX"
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_477():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="runs a series of test network connections to ncbi and gisaid to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_478():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="RUNS A SERIES OF TEST NETWORK CONNECTIONS TO NCBI AND GISAID TO TROUBLESHOOT SUBMISSION ISSUES."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_479():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = None

	return parser

def x_args_parser__mutmut_480():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		None,
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_481():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=None,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_482():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description=None
	)

	return parser

def x_args_parser__mutmut_483():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_484():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_485():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		)

	return parser

def x_args_parser__mutmut_486():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"XXversionXX",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_487():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"VERSION",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Print version."
	)

	return parser

def x_args_parser__mutmut_488():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="XXPrint version.XX"
	)

	return parser

def x_args_parser__mutmut_489():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="print version."
	)

	return parser

def x_args_parser__mutmut_490():
	"""
	Argument parser setup and build.
	"""
	parser = argparse.ArgumentParser(formatter_class=argparse.ArgumentDefaultsHelpFormatter,
									description="Genomic tool to simplify/automate the process of submitting organism samples to public repositories. With built-in tools to create/submit/link/log organism samples for the databases: BioSample, SRA, GenBank, and GISAID.")
	database_parser = argparse.ArgumentParser(add_help=False)
	organism_parser = argparse.ArgumentParser(add_help=False)
	validate_parser = argparse.ArgumentParser(add_help=False)
	submission_name_parser = argparse.ArgumentParser(add_help=False)
	submission_dir_parser = argparse.ArgumentParser(add_help=False)
	upload_log_submission_name_parser = argparse.ArgumentParser(add_help=False)
	config_file_parser = argparse.ArgumentParser(add_help=False)
	decryption_key_parser = argparse.ArgumentParser(add_help=False)
	multiple_decryption_key_parser = argparse.ArgumentParser(add_help=False)
	encrypt_config_file_parser = argparse.ArgumentParser(add_help=False)
	encrypt_with_key_parser = argparse.ArgumentParser(add_help=False)
	file_parser = argparse.ArgumentParser(add_help=False)
	test_parser = argparse.ArgumentParser(add_help=False)
	publication_parser = argparse.ArgumentParser(add_help=False)
	database_parser.add_argument("--biosample", "-b",
		help="Create/Submit BioSample data.",
		action="store_const",
		const="BIOSAMPLE",
		default="")
	database_parser.add_argument("--sra", "-s",
		help="Create/Submit SRA data.",
		action="store_const",
		const="SRA",
		default="")
	database_parser.add_argument("--genbank", "-n",
		help="Create/Submit GenBank data. (requires --fasta_file)",
		action="store_const",
		const="GENBANK",
		default="")
	database_parser.add_argument("--gisaid", "-g",
		help="Create/Submit GISAID data. (requires --fasta_file)",
		action="store_const",
		const="GISAID",
		default="")
	organism_parser.add_argument("--organism",
		help="Type of organism data. Listed organism options have unique submissions options/processes, if your specific organism is not listed, use 'OTHER' for options available to all organisms.",
		choices=ORGANISM_CHOICES,
		default="",
		required=True)
	validate_parser.add_argument("--skip_validation",
		help="Skip initial validation for metadata file. Validation will still occur for the 'config_file' and for any subsequent submissions made via 'submission_status'. Warning, this can cause unexpected errors using SeqSender if required columns are missing.",
		action="store_const",
		default=False,
		const=True)
	submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. Reusing the same name can cause issues during the submission process. A folder will be created at: 'submission_dir/submission_name'.",
		required=True)
	upload_log_submission_name_parser.add_argument("--submission_name",
		help="Unique name for the submission of your data. This is an optional field if you want Seqsender to only update the specified submission in the 'submission_log.csv'.")
	submission_dir_parser.add_argument("--submission_dir",
		help="Output directory where all files for your submission will be stored. A folder will be created at '<submission_dir>/<submission_name>'; this is the location where: all of the submission files will be created, SeqSender will stage each step of the submission process automatically, and where SeqSender will generate all the output from your submission.",
		required=True)
	config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. SeqSender will store this file location in your 'submission_log.csv' where it will use it to manage your submission, be careful when modifying and ensure SeqSender maintains access to this file. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<config_file>'.",
		required=True)
	decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file.",
		required=True)
	multiple_decryption_key_parser.add_argument("--key",
		help="Key provided to access credentials stored in config file. Multiple keys may be provided if multiple config files are being checked.",
		nargs='+',
		required=True)
	encrypt_config_file_parser.add_argument("--config_file",
		help="Config file to be used in the creation/submission of your samples. Input full file path.",
		required=True)
	encrypt_with_key_parser.add_argument("--key",
		help="Encryption key to use when encrypting your credentials. Allows the user to reuse the encryption key for multiple config files for users where multiple config files are needed for different submission criteria.",
		default=None,
		required=False)
	file_parser.add_argument("--metadata_file",
		help="Metadata file to be used in the creation/submission of your samples. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<metadata_file>'.",
		required=True)
	file_parser.add_argument("--fasta_file",
		help="Fasta file used to generate submission files; fasta header should match the column 'sequence_name' stored in your metadata. Input either full file path or if just file name it must be stored at '<submission_dir>/<submission_name>/<fasta_file>'.",
		default = None)
	file_parser.add_argument("--table2asn",
		help="Perform a table2asn submission instead of GenBank FTP submission for organism choices 'FLU' or 'COV'.",
		action="store_const",
		default=False,
		const=True)
	file_parser.add_argument("--gff_file",
		help="Annotation file only available for table2asn submissions. (requires '--table2asn' for organism choices 'FLU', or 'COV').",
		default=None)
	test_parser.add_argument("--test",
		help="Perform a test submission.",
		action="store_const",
		default=False,
		const=True)
	publication_parser.add_argument("--publication_title",
		help="Publication Title associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None)
	publication_parser.add_argument("--publication_status",
		help="Status of publication associated with sample submission. For GenBank only, overwrites value given via config file.",
		default=None,
		choices=["Unpublished", "In-press", "Published"])

	# Create the submodule commands
	subparser_modules = parser.add_subparsers(dest="command")

	# prep command
	prep_module = subparser_modules.add_parser(
		"prep",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required to submit to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, file_parser, validate_parser, publication_parser]
	)

	# submit command
	submit_module = subparser_modules.add_parser(
		"submit",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Generate all files required and begin the submission process to databases selected.",
		parents=[database_parser, organism_parser, submission_name_parser, submission_dir_parser, config_file_parser, decryption_key_parser, file_parser, test_parser, validate_parser, publication_parser]
	)

	# check_submission_status command
	update_module = subparser_modules.add_parser(
		"submission_status",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Checks the submission status for (all/specified <submission_name>) submission('s) which will for each database: update the status of the submission('s), download output file('s), submit to subsequent specified databases if linking information requires output of previous database('s).",
		parents=[submission_dir_parser, upload_log_submission_name_parser, multiple_decryption_key_parser]
	)

	encryption_module = subparser_modules.add_parser(
		"load_credentials",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Loads and encrypts your submission credentials into your submission config file. Provides a secret key to securely access your credentials for submission.",
		parents=[encrypt_config_file_parser, database_parser, encrypt_with_key_parser]
	)

	# Generate test data command
	test_output_module = subparser_modules.add_parser(
		"test_data",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Returns a set of test data examples (config_file, metadata_file, fasta_file, etc.) to the specified 'submission_dir' based on organism and database selections.",
		parents=[database_parser, organism_parser, submission_dir_parser]
	)

	# biosample xml download command
	biosample_xml_module = subparser_modules.add_parser(
		"update_biosample",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Downloads the BioSample Package XML from NCBI and updates SeqSender's metadata schema options for the BioSample database."
	)

	# check_submission_status command
	test_network_module = subparser_modules.add_parser(
		"test_network_connection",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="Runs a series of test network connections to NCBI and GISAID to troubleshoot submission issues."
	)

	# version command
	version_module = subparser_modules.add_parser(
		"version",
		formatter_class=argparse.ArgumentDefaultsHelpFormatter,
		description="PRINT VERSION."
	)

	return parser

x_args_parser__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_args_parser__mutmut_1': x_args_parser__mutmut_1, 
    'x_args_parser__mutmut_2': x_args_parser__mutmut_2, 
    'x_args_parser__mutmut_3': x_args_parser__mutmut_3, 
    'x_args_parser__mutmut_4': x_args_parser__mutmut_4, 
    'x_args_parser__mutmut_5': x_args_parser__mutmut_5, 
    'x_args_parser__mutmut_6': x_args_parser__mutmut_6, 
    'x_args_parser__mutmut_7': x_args_parser__mutmut_7, 
    'x_args_parser__mutmut_8': x_args_parser__mutmut_8, 
    'x_args_parser__mutmut_9': x_args_parser__mutmut_9, 
    'x_args_parser__mutmut_10': x_args_parser__mutmut_10, 
    'x_args_parser__mutmut_11': x_args_parser__mutmut_11, 
    'x_args_parser__mutmut_12': x_args_parser__mutmut_12, 
    'x_args_parser__mutmut_13': x_args_parser__mutmut_13, 
    'x_args_parser__mutmut_14': x_args_parser__mutmut_14, 
    'x_args_parser__mutmut_15': x_args_parser__mutmut_15, 
    'x_args_parser__mutmut_16': x_args_parser__mutmut_16, 
    'x_args_parser__mutmut_17': x_args_parser__mutmut_17, 
    'x_args_parser__mutmut_18': x_args_parser__mutmut_18, 
    'x_args_parser__mutmut_19': x_args_parser__mutmut_19, 
    'x_args_parser__mutmut_20': x_args_parser__mutmut_20, 
    'x_args_parser__mutmut_21': x_args_parser__mutmut_21, 
    'x_args_parser__mutmut_22': x_args_parser__mutmut_22, 
    'x_args_parser__mutmut_23': x_args_parser__mutmut_23, 
    'x_args_parser__mutmut_24': x_args_parser__mutmut_24, 
    'x_args_parser__mutmut_25': x_args_parser__mutmut_25, 
    'x_args_parser__mutmut_26': x_args_parser__mutmut_26, 
    'x_args_parser__mutmut_27': x_args_parser__mutmut_27, 
    'x_args_parser__mutmut_28': x_args_parser__mutmut_28, 
    'x_args_parser__mutmut_29': x_args_parser__mutmut_29, 
    'x_args_parser__mutmut_30': x_args_parser__mutmut_30, 
    'x_args_parser__mutmut_31': x_args_parser__mutmut_31, 
    'x_args_parser__mutmut_32': x_args_parser__mutmut_32, 
    'x_args_parser__mutmut_33': x_args_parser__mutmut_33, 
    'x_args_parser__mutmut_34': x_args_parser__mutmut_34, 
    'x_args_parser__mutmut_35': x_args_parser__mutmut_35, 
    'x_args_parser__mutmut_36': x_args_parser__mutmut_36, 
    'x_args_parser__mutmut_37': x_args_parser__mutmut_37, 
    'x_args_parser__mutmut_38': x_args_parser__mutmut_38, 
    'x_args_parser__mutmut_39': x_args_parser__mutmut_39, 
    'x_args_parser__mutmut_40': x_args_parser__mutmut_40, 
    'x_args_parser__mutmut_41': x_args_parser__mutmut_41, 
    'x_args_parser__mutmut_42': x_args_parser__mutmut_42, 
    'x_args_parser__mutmut_43': x_args_parser__mutmut_43, 
    'x_args_parser__mutmut_44': x_args_parser__mutmut_44, 
    'x_args_parser__mutmut_45': x_args_parser__mutmut_45, 
    'x_args_parser__mutmut_46': x_args_parser__mutmut_46, 
    'x_args_parser__mutmut_47': x_args_parser__mutmut_47, 
    'x_args_parser__mutmut_48': x_args_parser__mutmut_48, 
    'x_args_parser__mutmut_49': x_args_parser__mutmut_49, 
    'x_args_parser__mutmut_50': x_args_parser__mutmut_50, 
    'x_args_parser__mutmut_51': x_args_parser__mutmut_51, 
    'x_args_parser__mutmut_52': x_args_parser__mutmut_52, 
    'x_args_parser__mutmut_53': x_args_parser__mutmut_53, 
    'x_args_parser__mutmut_54': x_args_parser__mutmut_54, 
    'x_args_parser__mutmut_55': x_args_parser__mutmut_55, 
    'x_args_parser__mutmut_56': x_args_parser__mutmut_56, 
    'x_args_parser__mutmut_57': x_args_parser__mutmut_57, 
    'x_args_parser__mutmut_58': x_args_parser__mutmut_58, 
    'x_args_parser__mutmut_59': x_args_parser__mutmut_59, 
    'x_args_parser__mutmut_60': x_args_parser__mutmut_60, 
    'x_args_parser__mutmut_61': x_args_parser__mutmut_61, 
    'x_args_parser__mutmut_62': x_args_parser__mutmut_62, 
    'x_args_parser__mutmut_63': x_args_parser__mutmut_63, 
    'x_args_parser__mutmut_64': x_args_parser__mutmut_64, 
    'x_args_parser__mutmut_65': x_args_parser__mutmut_65, 
    'x_args_parser__mutmut_66': x_args_parser__mutmut_66, 
    'x_args_parser__mutmut_67': x_args_parser__mutmut_67, 
    'x_args_parser__mutmut_68': x_args_parser__mutmut_68, 
    'x_args_parser__mutmut_69': x_args_parser__mutmut_69, 
    'x_args_parser__mutmut_70': x_args_parser__mutmut_70, 
    'x_args_parser__mutmut_71': x_args_parser__mutmut_71, 
    'x_args_parser__mutmut_72': x_args_parser__mutmut_72, 
    'x_args_parser__mutmut_73': x_args_parser__mutmut_73, 
    'x_args_parser__mutmut_74': x_args_parser__mutmut_74, 
    'x_args_parser__mutmut_75': x_args_parser__mutmut_75, 
    'x_args_parser__mutmut_76': x_args_parser__mutmut_76, 
    'x_args_parser__mutmut_77': x_args_parser__mutmut_77, 
    'x_args_parser__mutmut_78': x_args_parser__mutmut_78, 
    'x_args_parser__mutmut_79': x_args_parser__mutmut_79, 
    'x_args_parser__mutmut_80': x_args_parser__mutmut_80, 
    'x_args_parser__mutmut_81': x_args_parser__mutmut_81, 
    'x_args_parser__mutmut_82': x_args_parser__mutmut_82, 
    'x_args_parser__mutmut_83': x_args_parser__mutmut_83, 
    'x_args_parser__mutmut_84': x_args_parser__mutmut_84, 
    'x_args_parser__mutmut_85': x_args_parser__mutmut_85, 
    'x_args_parser__mutmut_86': x_args_parser__mutmut_86, 
    'x_args_parser__mutmut_87': x_args_parser__mutmut_87, 
    'x_args_parser__mutmut_88': x_args_parser__mutmut_88, 
    'x_args_parser__mutmut_89': x_args_parser__mutmut_89, 
    'x_args_parser__mutmut_90': x_args_parser__mutmut_90, 
    'x_args_parser__mutmut_91': x_args_parser__mutmut_91, 
    'x_args_parser__mutmut_92': x_args_parser__mutmut_92, 
    'x_args_parser__mutmut_93': x_args_parser__mutmut_93, 
    'x_args_parser__mutmut_94': x_args_parser__mutmut_94, 
    'x_args_parser__mutmut_95': x_args_parser__mutmut_95, 
    'x_args_parser__mutmut_96': x_args_parser__mutmut_96, 
    'x_args_parser__mutmut_97': x_args_parser__mutmut_97, 
    'x_args_parser__mutmut_98': x_args_parser__mutmut_98, 
    'x_args_parser__mutmut_99': x_args_parser__mutmut_99, 
    'x_args_parser__mutmut_100': x_args_parser__mutmut_100, 
    'x_args_parser__mutmut_101': x_args_parser__mutmut_101, 
    'x_args_parser__mutmut_102': x_args_parser__mutmut_102, 
    'x_args_parser__mutmut_103': x_args_parser__mutmut_103, 
    'x_args_parser__mutmut_104': x_args_parser__mutmut_104, 
    'x_args_parser__mutmut_105': x_args_parser__mutmut_105, 
    'x_args_parser__mutmut_106': x_args_parser__mutmut_106, 
    'x_args_parser__mutmut_107': x_args_parser__mutmut_107, 
    'x_args_parser__mutmut_108': x_args_parser__mutmut_108, 
    'x_args_parser__mutmut_109': x_args_parser__mutmut_109, 
    'x_args_parser__mutmut_110': x_args_parser__mutmut_110, 
    'x_args_parser__mutmut_111': x_args_parser__mutmut_111, 
    'x_args_parser__mutmut_112': x_args_parser__mutmut_112, 
    'x_args_parser__mutmut_113': x_args_parser__mutmut_113, 
    'x_args_parser__mutmut_114': x_args_parser__mutmut_114, 
    'x_args_parser__mutmut_115': x_args_parser__mutmut_115, 
    'x_args_parser__mutmut_116': x_args_parser__mutmut_116, 
    'x_args_parser__mutmut_117': x_args_parser__mutmut_117, 
    'x_args_parser__mutmut_118': x_args_parser__mutmut_118, 
    'x_args_parser__mutmut_119': x_args_parser__mutmut_119, 
    'x_args_parser__mutmut_120': x_args_parser__mutmut_120, 
    'x_args_parser__mutmut_121': x_args_parser__mutmut_121, 
    'x_args_parser__mutmut_122': x_args_parser__mutmut_122, 
    'x_args_parser__mutmut_123': x_args_parser__mutmut_123, 
    'x_args_parser__mutmut_124': x_args_parser__mutmut_124, 
    'x_args_parser__mutmut_125': x_args_parser__mutmut_125, 
    'x_args_parser__mutmut_126': x_args_parser__mutmut_126, 
    'x_args_parser__mutmut_127': x_args_parser__mutmut_127, 
    'x_args_parser__mutmut_128': x_args_parser__mutmut_128, 
    'x_args_parser__mutmut_129': x_args_parser__mutmut_129, 
    'x_args_parser__mutmut_130': x_args_parser__mutmut_130, 
    'x_args_parser__mutmut_131': x_args_parser__mutmut_131, 
    'x_args_parser__mutmut_132': x_args_parser__mutmut_132, 
    'x_args_parser__mutmut_133': x_args_parser__mutmut_133, 
    'x_args_parser__mutmut_134': x_args_parser__mutmut_134, 
    'x_args_parser__mutmut_135': x_args_parser__mutmut_135, 
    'x_args_parser__mutmut_136': x_args_parser__mutmut_136, 
    'x_args_parser__mutmut_137': x_args_parser__mutmut_137, 
    'x_args_parser__mutmut_138': x_args_parser__mutmut_138, 
    'x_args_parser__mutmut_139': x_args_parser__mutmut_139, 
    'x_args_parser__mutmut_140': x_args_parser__mutmut_140, 
    'x_args_parser__mutmut_141': x_args_parser__mutmut_141, 
    'x_args_parser__mutmut_142': x_args_parser__mutmut_142, 
    'x_args_parser__mutmut_143': x_args_parser__mutmut_143, 
    'x_args_parser__mutmut_144': x_args_parser__mutmut_144, 
    'x_args_parser__mutmut_145': x_args_parser__mutmut_145, 
    'x_args_parser__mutmut_146': x_args_parser__mutmut_146, 
    'x_args_parser__mutmut_147': x_args_parser__mutmut_147, 
    'x_args_parser__mutmut_148': x_args_parser__mutmut_148, 
    'x_args_parser__mutmut_149': x_args_parser__mutmut_149, 
    'x_args_parser__mutmut_150': x_args_parser__mutmut_150, 
    'x_args_parser__mutmut_151': x_args_parser__mutmut_151, 
    'x_args_parser__mutmut_152': x_args_parser__mutmut_152, 
    'x_args_parser__mutmut_153': x_args_parser__mutmut_153, 
    'x_args_parser__mutmut_154': x_args_parser__mutmut_154, 
    'x_args_parser__mutmut_155': x_args_parser__mutmut_155, 
    'x_args_parser__mutmut_156': x_args_parser__mutmut_156, 
    'x_args_parser__mutmut_157': x_args_parser__mutmut_157, 
    'x_args_parser__mutmut_158': x_args_parser__mutmut_158, 
    'x_args_parser__mutmut_159': x_args_parser__mutmut_159, 
    'x_args_parser__mutmut_160': x_args_parser__mutmut_160, 
    'x_args_parser__mutmut_161': x_args_parser__mutmut_161, 
    'x_args_parser__mutmut_162': x_args_parser__mutmut_162, 
    'x_args_parser__mutmut_163': x_args_parser__mutmut_163, 
    'x_args_parser__mutmut_164': x_args_parser__mutmut_164, 
    'x_args_parser__mutmut_165': x_args_parser__mutmut_165, 
    'x_args_parser__mutmut_166': x_args_parser__mutmut_166, 
    'x_args_parser__mutmut_167': x_args_parser__mutmut_167, 
    'x_args_parser__mutmut_168': x_args_parser__mutmut_168, 
    'x_args_parser__mutmut_169': x_args_parser__mutmut_169, 
    'x_args_parser__mutmut_170': x_args_parser__mutmut_170, 
    'x_args_parser__mutmut_171': x_args_parser__mutmut_171, 
    'x_args_parser__mutmut_172': x_args_parser__mutmut_172, 
    'x_args_parser__mutmut_173': x_args_parser__mutmut_173, 
    'x_args_parser__mutmut_174': x_args_parser__mutmut_174, 
    'x_args_parser__mutmut_175': x_args_parser__mutmut_175, 
    'x_args_parser__mutmut_176': x_args_parser__mutmut_176, 
    'x_args_parser__mutmut_177': x_args_parser__mutmut_177, 
    'x_args_parser__mutmut_178': x_args_parser__mutmut_178, 
    'x_args_parser__mutmut_179': x_args_parser__mutmut_179, 
    'x_args_parser__mutmut_180': x_args_parser__mutmut_180, 
    'x_args_parser__mutmut_181': x_args_parser__mutmut_181, 
    'x_args_parser__mutmut_182': x_args_parser__mutmut_182, 
    'x_args_parser__mutmut_183': x_args_parser__mutmut_183, 
    'x_args_parser__mutmut_184': x_args_parser__mutmut_184, 
    'x_args_parser__mutmut_185': x_args_parser__mutmut_185, 
    'x_args_parser__mutmut_186': x_args_parser__mutmut_186, 
    'x_args_parser__mutmut_187': x_args_parser__mutmut_187, 
    'x_args_parser__mutmut_188': x_args_parser__mutmut_188, 
    'x_args_parser__mutmut_189': x_args_parser__mutmut_189, 
    'x_args_parser__mutmut_190': x_args_parser__mutmut_190, 
    'x_args_parser__mutmut_191': x_args_parser__mutmut_191, 
    'x_args_parser__mutmut_192': x_args_parser__mutmut_192, 
    'x_args_parser__mutmut_193': x_args_parser__mutmut_193, 
    'x_args_parser__mutmut_194': x_args_parser__mutmut_194, 
    'x_args_parser__mutmut_195': x_args_parser__mutmut_195, 
    'x_args_parser__mutmut_196': x_args_parser__mutmut_196, 
    'x_args_parser__mutmut_197': x_args_parser__mutmut_197, 
    'x_args_parser__mutmut_198': x_args_parser__mutmut_198, 
    'x_args_parser__mutmut_199': x_args_parser__mutmut_199, 
    'x_args_parser__mutmut_200': x_args_parser__mutmut_200, 
    'x_args_parser__mutmut_201': x_args_parser__mutmut_201, 
    'x_args_parser__mutmut_202': x_args_parser__mutmut_202, 
    'x_args_parser__mutmut_203': x_args_parser__mutmut_203, 
    'x_args_parser__mutmut_204': x_args_parser__mutmut_204, 
    'x_args_parser__mutmut_205': x_args_parser__mutmut_205, 
    'x_args_parser__mutmut_206': x_args_parser__mutmut_206, 
    'x_args_parser__mutmut_207': x_args_parser__mutmut_207, 
    'x_args_parser__mutmut_208': x_args_parser__mutmut_208, 
    'x_args_parser__mutmut_209': x_args_parser__mutmut_209, 
    'x_args_parser__mutmut_210': x_args_parser__mutmut_210, 
    'x_args_parser__mutmut_211': x_args_parser__mutmut_211, 
    'x_args_parser__mutmut_212': x_args_parser__mutmut_212, 
    'x_args_parser__mutmut_213': x_args_parser__mutmut_213, 
    'x_args_parser__mutmut_214': x_args_parser__mutmut_214, 
    'x_args_parser__mutmut_215': x_args_parser__mutmut_215, 
    'x_args_parser__mutmut_216': x_args_parser__mutmut_216, 
    'x_args_parser__mutmut_217': x_args_parser__mutmut_217, 
    'x_args_parser__mutmut_218': x_args_parser__mutmut_218, 
    'x_args_parser__mutmut_219': x_args_parser__mutmut_219, 
    'x_args_parser__mutmut_220': x_args_parser__mutmut_220, 
    'x_args_parser__mutmut_221': x_args_parser__mutmut_221, 
    'x_args_parser__mutmut_222': x_args_parser__mutmut_222, 
    'x_args_parser__mutmut_223': x_args_parser__mutmut_223, 
    'x_args_parser__mutmut_224': x_args_parser__mutmut_224, 
    'x_args_parser__mutmut_225': x_args_parser__mutmut_225, 
    'x_args_parser__mutmut_226': x_args_parser__mutmut_226, 
    'x_args_parser__mutmut_227': x_args_parser__mutmut_227, 
    'x_args_parser__mutmut_228': x_args_parser__mutmut_228, 
    'x_args_parser__mutmut_229': x_args_parser__mutmut_229, 
    'x_args_parser__mutmut_230': x_args_parser__mutmut_230, 
    'x_args_parser__mutmut_231': x_args_parser__mutmut_231, 
    'x_args_parser__mutmut_232': x_args_parser__mutmut_232, 
    'x_args_parser__mutmut_233': x_args_parser__mutmut_233, 
    'x_args_parser__mutmut_234': x_args_parser__mutmut_234, 
    'x_args_parser__mutmut_235': x_args_parser__mutmut_235, 
    'x_args_parser__mutmut_236': x_args_parser__mutmut_236, 
    'x_args_parser__mutmut_237': x_args_parser__mutmut_237, 
    'x_args_parser__mutmut_238': x_args_parser__mutmut_238, 
    'x_args_parser__mutmut_239': x_args_parser__mutmut_239, 
    'x_args_parser__mutmut_240': x_args_parser__mutmut_240, 
    'x_args_parser__mutmut_241': x_args_parser__mutmut_241, 
    'x_args_parser__mutmut_242': x_args_parser__mutmut_242, 
    'x_args_parser__mutmut_243': x_args_parser__mutmut_243, 
    'x_args_parser__mutmut_244': x_args_parser__mutmut_244, 
    'x_args_parser__mutmut_245': x_args_parser__mutmut_245, 
    'x_args_parser__mutmut_246': x_args_parser__mutmut_246, 
    'x_args_parser__mutmut_247': x_args_parser__mutmut_247, 
    'x_args_parser__mutmut_248': x_args_parser__mutmut_248, 
    'x_args_parser__mutmut_249': x_args_parser__mutmut_249, 
    'x_args_parser__mutmut_250': x_args_parser__mutmut_250, 
    'x_args_parser__mutmut_251': x_args_parser__mutmut_251, 
    'x_args_parser__mutmut_252': x_args_parser__mutmut_252, 
    'x_args_parser__mutmut_253': x_args_parser__mutmut_253, 
    'x_args_parser__mutmut_254': x_args_parser__mutmut_254, 
    'x_args_parser__mutmut_255': x_args_parser__mutmut_255, 
    'x_args_parser__mutmut_256': x_args_parser__mutmut_256, 
    'x_args_parser__mutmut_257': x_args_parser__mutmut_257, 
    'x_args_parser__mutmut_258': x_args_parser__mutmut_258, 
    'x_args_parser__mutmut_259': x_args_parser__mutmut_259, 
    'x_args_parser__mutmut_260': x_args_parser__mutmut_260, 
    'x_args_parser__mutmut_261': x_args_parser__mutmut_261, 
    'x_args_parser__mutmut_262': x_args_parser__mutmut_262, 
    'x_args_parser__mutmut_263': x_args_parser__mutmut_263, 
    'x_args_parser__mutmut_264': x_args_parser__mutmut_264, 
    'x_args_parser__mutmut_265': x_args_parser__mutmut_265, 
    'x_args_parser__mutmut_266': x_args_parser__mutmut_266, 
    'x_args_parser__mutmut_267': x_args_parser__mutmut_267, 
    'x_args_parser__mutmut_268': x_args_parser__mutmut_268, 
    'x_args_parser__mutmut_269': x_args_parser__mutmut_269, 
    'x_args_parser__mutmut_270': x_args_parser__mutmut_270, 
    'x_args_parser__mutmut_271': x_args_parser__mutmut_271, 
    'x_args_parser__mutmut_272': x_args_parser__mutmut_272, 
    'x_args_parser__mutmut_273': x_args_parser__mutmut_273, 
    'x_args_parser__mutmut_274': x_args_parser__mutmut_274, 
    'x_args_parser__mutmut_275': x_args_parser__mutmut_275, 
    'x_args_parser__mutmut_276': x_args_parser__mutmut_276, 
    'x_args_parser__mutmut_277': x_args_parser__mutmut_277, 
    'x_args_parser__mutmut_278': x_args_parser__mutmut_278, 
    'x_args_parser__mutmut_279': x_args_parser__mutmut_279, 
    'x_args_parser__mutmut_280': x_args_parser__mutmut_280, 
    'x_args_parser__mutmut_281': x_args_parser__mutmut_281, 
    'x_args_parser__mutmut_282': x_args_parser__mutmut_282, 
    'x_args_parser__mutmut_283': x_args_parser__mutmut_283, 
    'x_args_parser__mutmut_284': x_args_parser__mutmut_284, 
    'x_args_parser__mutmut_285': x_args_parser__mutmut_285, 
    'x_args_parser__mutmut_286': x_args_parser__mutmut_286, 
    'x_args_parser__mutmut_287': x_args_parser__mutmut_287, 
    'x_args_parser__mutmut_288': x_args_parser__mutmut_288, 
    'x_args_parser__mutmut_289': x_args_parser__mutmut_289, 
    'x_args_parser__mutmut_290': x_args_parser__mutmut_290, 
    'x_args_parser__mutmut_291': x_args_parser__mutmut_291, 
    'x_args_parser__mutmut_292': x_args_parser__mutmut_292, 
    'x_args_parser__mutmut_293': x_args_parser__mutmut_293, 
    'x_args_parser__mutmut_294': x_args_parser__mutmut_294, 
    'x_args_parser__mutmut_295': x_args_parser__mutmut_295, 
    'x_args_parser__mutmut_296': x_args_parser__mutmut_296, 
    'x_args_parser__mutmut_297': x_args_parser__mutmut_297, 
    'x_args_parser__mutmut_298': x_args_parser__mutmut_298, 
    'x_args_parser__mutmut_299': x_args_parser__mutmut_299, 
    'x_args_parser__mutmut_300': x_args_parser__mutmut_300, 
    'x_args_parser__mutmut_301': x_args_parser__mutmut_301, 
    'x_args_parser__mutmut_302': x_args_parser__mutmut_302, 
    'x_args_parser__mutmut_303': x_args_parser__mutmut_303, 
    'x_args_parser__mutmut_304': x_args_parser__mutmut_304, 
    'x_args_parser__mutmut_305': x_args_parser__mutmut_305, 
    'x_args_parser__mutmut_306': x_args_parser__mutmut_306, 
    'x_args_parser__mutmut_307': x_args_parser__mutmut_307, 
    'x_args_parser__mutmut_308': x_args_parser__mutmut_308, 
    'x_args_parser__mutmut_309': x_args_parser__mutmut_309, 
    'x_args_parser__mutmut_310': x_args_parser__mutmut_310, 
    'x_args_parser__mutmut_311': x_args_parser__mutmut_311, 
    'x_args_parser__mutmut_312': x_args_parser__mutmut_312, 
    'x_args_parser__mutmut_313': x_args_parser__mutmut_313, 
    'x_args_parser__mutmut_314': x_args_parser__mutmut_314, 
    'x_args_parser__mutmut_315': x_args_parser__mutmut_315, 
    'x_args_parser__mutmut_316': x_args_parser__mutmut_316, 
    'x_args_parser__mutmut_317': x_args_parser__mutmut_317, 
    'x_args_parser__mutmut_318': x_args_parser__mutmut_318, 
    'x_args_parser__mutmut_319': x_args_parser__mutmut_319, 
    'x_args_parser__mutmut_320': x_args_parser__mutmut_320, 
    'x_args_parser__mutmut_321': x_args_parser__mutmut_321, 
    'x_args_parser__mutmut_322': x_args_parser__mutmut_322, 
    'x_args_parser__mutmut_323': x_args_parser__mutmut_323, 
    'x_args_parser__mutmut_324': x_args_parser__mutmut_324, 
    'x_args_parser__mutmut_325': x_args_parser__mutmut_325, 
    'x_args_parser__mutmut_326': x_args_parser__mutmut_326, 
    'x_args_parser__mutmut_327': x_args_parser__mutmut_327, 
    'x_args_parser__mutmut_328': x_args_parser__mutmut_328, 
    'x_args_parser__mutmut_329': x_args_parser__mutmut_329, 
    'x_args_parser__mutmut_330': x_args_parser__mutmut_330, 
    'x_args_parser__mutmut_331': x_args_parser__mutmut_331, 
    'x_args_parser__mutmut_332': x_args_parser__mutmut_332, 
    'x_args_parser__mutmut_333': x_args_parser__mutmut_333, 
    'x_args_parser__mutmut_334': x_args_parser__mutmut_334, 
    'x_args_parser__mutmut_335': x_args_parser__mutmut_335, 
    'x_args_parser__mutmut_336': x_args_parser__mutmut_336, 
    'x_args_parser__mutmut_337': x_args_parser__mutmut_337, 
    'x_args_parser__mutmut_338': x_args_parser__mutmut_338, 
    'x_args_parser__mutmut_339': x_args_parser__mutmut_339, 
    'x_args_parser__mutmut_340': x_args_parser__mutmut_340, 
    'x_args_parser__mutmut_341': x_args_parser__mutmut_341, 
    'x_args_parser__mutmut_342': x_args_parser__mutmut_342, 
    'x_args_parser__mutmut_343': x_args_parser__mutmut_343, 
    'x_args_parser__mutmut_344': x_args_parser__mutmut_344, 
    'x_args_parser__mutmut_345': x_args_parser__mutmut_345, 
    'x_args_parser__mutmut_346': x_args_parser__mutmut_346, 
    'x_args_parser__mutmut_347': x_args_parser__mutmut_347, 
    'x_args_parser__mutmut_348': x_args_parser__mutmut_348, 
    'x_args_parser__mutmut_349': x_args_parser__mutmut_349, 
    'x_args_parser__mutmut_350': x_args_parser__mutmut_350, 
    'x_args_parser__mutmut_351': x_args_parser__mutmut_351, 
    'x_args_parser__mutmut_352': x_args_parser__mutmut_352, 
    'x_args_parser__mutmut_353': x_args_parser__mutmut_353, 
    'x_args_parser__mutmut_354': x_args_parser__mutmut_354, 
    'x_args_parser__mutmut_355': x_args_parser__mutmut_355, 
    'x_args_parser__mutmut_356': x_args_parser__mutmut_356, 
    'x_args_parser__mutmut_357': x_args_parser__mutmut_357, 
    'x_args_parser__mutmut_358': x_args_parser__mutmut_358, 
    'x_args_parser__mutmut_359': x_args_parser__mutmut_359, 
    'x_args_parser__mutmut_360': x_args_parser__mutmut_360, 
    'x_args_parser__mutmut_361': x_args_parser__mutmut_361, 
    'x_args_parser__mutmut_362': x_args_parser__mutmut_362, 
    'x_args_parser__mutmut_363': x_args_parser__mutmut_363, 
    'x_args_parser__mutmut_364': x_args_parser__mutmut_364, 
    'x_args_parser__mutmut_365': x_args_parser__mutmut_365, 
    'x_args_parser__mutmut_366': x_args_parser__mutmut_366, 
    'x_args_parser__mutmut_367': x_args_parser__mutmut_367, 
    'x_args_parser__mutmut_368': x_args_parser__mutmut_368, 
    'x_args_parser__mutmut_369': x_args_parser__mutmut_369, 
    'x_args_parser__mutmut_370': x_args_parser__mutmut_370, 
    'x_args_parser__mutmut_371': x_args_parser__mutmut_371, 
    'x_args_parser__mutmut_372': x_args_parser__mutmut_372, 
    'x_args_parser__mutmut_373': x_args_parser__mutmut_373, 
    'x_args_parser__mutmut_374': x_args_parser__mutmut_374, 
    'x_args_parser__mutmut_375': x_args_parser__mutmut_375, 
    'x_args_parser__mutmut_376': x_args_parser__mutmut_376, 
    'x_args_parser__mutmut_377': x_args_parser__mutmut_377, 
    'x_args_parser__mutmut_378': x_args_parser__mutmut_378, 
    'x_args_parser__mutmut_379': x_args_parser__mutmut_379, 
    'x_args_parser__mutmut_380': x_args_parser__mutmut_380, 
    'x_args_parser__mutmut_381': x_args_parser__mutmut_381, 
    'x_args_parser__mutmut_382': x_args_parser__mutmut_382, 
    'x_args_parser__mutmut_383': x_args_parser__mutmut_383, 
    'x_args_parser__mutmut_384': x_args_parser__mutmut_384, 
    'x_args_parser__mutmut_385': x_args_parser__mutmut_385, 
    'x_args_parser__mutmut_386': x_args_parser__mutmut_386, 
    'x_args_parser__mutmut_387': x_args_parser__mutmut_387, 
    'x_args_parser__mutmut_388': x_args_parser__mutmut_388, 
    'x_args_parser__mutmut_389': x_args_parser__mutmut_389, 
    'x_args_parser__mutmut_390': x_args_parser__mutmut_390, 
    'x_args_parser__mutmut_391': x_args_parser__mutmut_391, 
    'x_args_parser__mutmut_392': x_args_parser__mutmut_392, 
    'x_args_parser__mutmut_393': x_args_parser__mutmut_393, 
    'x_args_parser__mutmut_394': x_args_parser__mutmut_394, 
    'x_args_parser__mutmut_395': x_args_parser__mutmut_395, 
    'x_args_parser__mutmut_396': x_args_parser__mutmut_396, 
    'x_args_parser__mutmut_397': x_args_parser__mutmut_397, 
    'x_args_parser__mutmut_398': x_args_parser__mutmut_398, 
    'x_args_parser__mutmut_399': x_args_parser__mutmut_399, 
    'x_args_parser__mutmut_400': x_args_parser__mutmut_400, 
    'x_args_parser__mutmut_401': x_args_parser__mutmut_401, 
    'x_args_parser__mutmut_402': x_args_parser__mutmut_402, 
    'x_args_parser__mutmut_403': x_args_parser__mutmut_403, 
    'x_args_parser__mutmut_404': x_args_parser__mutmut_404, 
    'x_args_parser__mutmut_405': x_args_parser__mutmut_405, 
    'x_args_parser__mutmut_406': x_args_parser__mutmut_406, 
    'x_args_parser__mutmut_407': x_args_parser__mutmut_407, 
    'x_args_parser__mutmut_408': x_args_parser__mutmut_408, 
    'x_args_parser__mutmut_409': x_args_parser__mutmut_409, 
    'x_args_parser__mutmut_410': x_args_parser__mutmut_410, 
    'x_args_parser__mutmut_411': x_args_parser__mutmut_411, 
    'x_args_parser__mutmut_412': x_args_parser__mutmut_412, 
    'x_args_parser__mutmut_413': x_args_parser__mutmut_413, 
    'x_args_parser__mutmut_414': x_args_parser__mutmut_414, 
    'x_args_parser__mutmut_415': x_args_parser__mutmut_415, 
    'x_args_parser__mutmut_416': x_args_parser__mutmut_416, 
    'x_args_parser__mutmut_417': x_args_parser__mutmut_417, 
    'x_args_parser__mutmut_418': x_args_parser__mutmut_418, 
    'x_args_parser__mutmut_419': x_args_parser__mutmut_419, 
    'x_args_parser__mutmut_420': x_args_parser__mutmut_420, 
    'x_args_parser__mutmut_421': x_args_parser__mutmut_421, 
    'x_args_parser__mutmut_422': x_args_parser__mutmut_422, 
    'x_args_parser__mutmut_423': x_args_parser__mutmut_423, 
    'x_args_parser__mutmut_424': x_args_parser__mutmut_424, 
    'x_args_parser__mutmut_425': x_args_parser__mutmut_425, 
    'x_args_parser__mutmut_426': x_args_parser__mutmut_426, 
    'x_args_parser__mutmut_427': x_args_parser__mutmut_427, 
    'x_args_parser__mutmut_428': x_args_parser__mutmut_428, 
    'x_args_parser__mutmut_429': x_args_parser__mutmut_429, 
    'x_args_parser__mutmut_430': x_args_parser__mutmut_430, 
    'x_args_parser__mutmut_431': x_args_parser__mutmut_431, 
    'x_args_parser__mutmut_432': x_args_parser__mutmut_432, 
    'x_args_parser__mutmut_433': x_args_parser__mutmut_433, 
    'x_args_parser__mutmut_434': x_args_parser__mutmut_434, 
    'x_args_parser__mutmut_435': x_args_parser__mutmut_435, 
    'x_args_parser__mutmut_436': x_args_parser__mutmut_436, 
    'x_args_parser__mutmut_437': x_args_parser__mutmut_437, 
    'x_args_parser__mutmut_438': x_args_parser__mutmut_438, 
    'x_args_parser__mutmut_439': x_args_parser__mutmut_439, 
    'x_args_parser__mutmut_440': x_args_parser__mutmut_440, 
    'x_args_parser__mutmut_441': x_args_parser__mutmut_441, 
    'x_args_parser__mutmut_442': x_args_parser__mutmut_442, 
    'x_args_parser__mutmut_443': x_args_parser__mutmut_443, 
    'x_args_parser__mutmut_444': x_args_parser__mutmut_444, 
    'x_args_parser__mutmut_445': x_args_parser__mutmut_445, 
    'x_args_parser__mutmut_446': x_args_parser__mutmut_446, 
    'x_args_parser__mutmut_447': x_args_parser__mutmut_447, 
    'x_args_parser__mutmut_448': x_args_parser__mutmut_448, 
    'x_args_parser__mutmut_449': x_args_parser__mutmut_449, 
    'x_args_parser__mutmut_450': x_args_parser__mutmut_450, 
    'x_args_parser__mutmut_451': x_args_parser__mutmut_451, 
    'x_args_parser__mutmut_452': x_args_parser__mutmut_452, 
    'x_args_parser__mutmut_453': x_args_parser__mutmut_453, 
    'x_args_parser__mutmut_454': x_args_parser__mutmut_454, 
    'x_args_parser__mutmut_455': x_args_parser__mutmut_455, 
    'x_args_parser__mutmut_456': x_args_parser__mutmut_456, 
    'x_args_parser__mutmut_457': x_args_parser__mutmut_457, 
    'x_args_parser__mutmut_458': x_args_parser__mutmut_458, 
    'x_args_parser__mutmut_459': x_args_parser__mutmut_459, 
    'x_args_parser__mutmut_460': x_args_parser__mutmut_460, 
    'x_args_parser__mutmut_461': x_args_parser__mutmut_461, 
    'x_args_parser__mutmut_462': x_args_parser__mutmut_462, 
    'x_args_parser__mutmut_463': x_args_parser__mutmut_463, 
    'x_args_parser__mutmut_464': x_args_parser__mutmut_464, 
    'x_args_parser__mutmut_465': x_args_parser__mutmut_465, 
    'x_args_parser__mutmut_466': x_args_parser__mutmut_466, 
    'x_args_parser__mutmut_467': x_args_parser__mutmut_467, 
    'x_args_parser__mutmut_468': x_args_parser__mutmut_468, 
    'x_args_parser__mutmut_469': x_args_parser__mutmut_469, 
    'x_args_parser__mutmut_470': x_args_parser__mutmut_470, 
    'x_args_parser__mutmut_471': x_args_parser__mutmut_471, 
    'x_args_parser__mutmut_472': x_args_parser__mutmut_472, 
    'x_args_parser__mutmut_473': x_args_parser__mutmut_473, 
    'x_args_parser__mutmut_474': x_args_parser__mutmut_474, 
    'x_args_parser__mutmut_475': x_args_parser__mutmut_475, 
    'x_args_parser__mutmut_476': x_args_parser__mutmut_476, 
    'x_args_parser__mutmut_477': x_args_parser__mutmut_477, 
    'x_args_parser__mutmut_478': x_args_parser__mutmut_478, 
    'x_args_parser__mutmut_479': x_args_parser__mutmut_479, 
    'x_args_parser__mutmut_480': x_args_parser__mutmut_480, 
    'x_args_parser__mutmut_481': x_args_parser__mutmut_481, 
    'x_args_parser__mutmut_482': x_args_parser__mutmut_482, 
    'x_args_parser__mutmut_483': x_args_parser__mutmut_483, 
    'x_args_parser__mutmut_484': x_args_parser__mutmut_484, 
    'x_args_parser__mutmut_485': x_args_parser__mutmut_485, 
    'x_args_parser__mutmut_486': x_args_parser__mutmut_486, 
    'x_args_parser__mutmut_487': x_args_parser__mutmut_487, 
    'x_args_parser__mutmut_488': x_args_parser__mutmut_488, 
    'x_args_parser__mutmut_489': x_args_parser__mutmut_489, 
    'x_args_parser__mutmut_490': x_args_parser__mutmut_490
}
x_args_parser__mutmut_orig.__name__ = 'x_args_parser'
